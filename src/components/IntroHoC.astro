---
import industrial from "@assets/img/industrial_area.jpg";
import warring from "@assets/NewTaipeiCityLogo.png";
---

<section class="section relative z-10 bg-white" id="intro-hoc">
	<!-- 跑馬燈 -->
	<div class="marquee-wrapper mb-10">
		<div id="marquee" style={`background-image: url(${warring.src})`}></div>
	</div>

  <div class="container mx-auto px-6">
    <div class="grid lg:grid-cols-2 gap-12 items-center max-w-6xl mx-auto">
      
      <!-- Text Content -->
      <div class="space-y-8 order-2 lg:order-1">
        <div>
          <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
			  關於
            <span class="text-[#573e00]">產業轉型</span>
		</h2>
          
          <div class="space-y-6 text-lg md:text-xl text-gray-700 leading-relaxed">
            <p>
			產業轉型是指一個國家或地區的產業結構發生顯著改變，從勞力密集或傳統產業轉向技術密集、知識密集或新興產業的過程。 它是一個多面向的過程，包含產業組織、技術裝備及產業規模的調整，目的是提升國家或地區的整體競爭力，適應全球市場和科技進步的需求
            </p>
          </div>
        </div>

        <!-- Features List -->
        <div class="space-y-4">
          <h3 class="text-2xl font-semibold text-gray-900 mb-4 pt-6"><span class="text-[#573e00]">產業轉型</span> 的三大策略</h3>
          <div class="grid gap-4">
            <div class="flex items-start gap-3">
              <div class="w-2 h-2 bg-[#573e00] rounded-full mt-3 flex-shrink-0"></div>
              <p class="text-lg text-gray-700">
                <span class="font-semibold">維新傳統產業 </span>透過數位轉型、技術升級等方式，讓傳統產業能夠適應市場變化，提高生產效率和競爭力。 
              </p>
            </div>
            <div class="flex items-start gap-3">
              <div class="w-2 h-2 bg-[#573e00] rounded-full mt-3 flex-shrink-0"></div>
              <p class="text-lg text-gray-700">
                <span class="font-semibold">鞏固主力產業 </span>持續投入資源，鞏固現有主力產業的地位，例如台灣過去在半導體產業的發展。  
              </p>
            </div>
            <div class="flex items-start gap-3">
              <div class="w-2 h-2 bg-[#573e00] rounded-full mt-3 flex-shrink-0"></div>
              <p class="text-lg text-gray-700">
                <span class="font-semibold">育成新興產業 </span>扶植新興產業的發展，例如台灣政府推動的「六大核心戰略產業」涵蓋了資訊及數位、資安卓越、綠電等領域，以掌握未來趨勢。
              </p>
            </div>
          </div>
        </div>




        <!-- Call to Action -->
        <div class="pt-4">
          <p class="text-xl text-gray-600 font-medium">
           	
          </p>
        </div>
      </div>

      <!-- Image -->
      <div class="order-1 lg:order-2">
        <div class="relative">
          <div class="absolute inset-0 bg-[#77B65A]/20 rounded-2xl transform rotate-3"></div>
          <div class="relative bg-white rounded-2xl overflow-hidden shadow-2xl">
            <img
              src={industrial.src}
              alt="Hour of Code 教學"
              class="w-full h-auto"
              loading="lazy"
            />
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="marquee-wrapper mt-10">
		<div id="marquee" style={`background-image: url(${warring.src})`}></div>
	</div>
</section>

<style>
  #container {
    padding: 6rem 0;
  }

  .marquee-wrapper {
    overflow: hidden;
    width: 100%;
    line-height: 0;

  }

  #marquee {
    --width: 20rem;
    --height: 5rem;
    width: calc(200vw);
    height: var(--height);
    background-size: auto var(--height);
    background-repeat: repeat-x;
    background-position: center;
    animation: marquee 20s linear infinite;
    display: block;
  }

  @keyframes marquee {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  @media (max-width: 768px) {
    #container {
      padding: 4rem 0;
    }
    
    #marquee {
      --width: 15rem;
      --height: 4rem;
    }
  }

  @media (max-width: 640px) {
    .grid {
      gap: 2rem;
    }
  }
</style>
